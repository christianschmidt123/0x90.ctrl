# javascript-ui

Use this skill for browser UI, frontend, and JavaScript application tasks.

Rules:
- Prefer async/await over promise chains when readable.
- Use fetch and built-in browser APIs first.
- Keep components small and state minimal.
- Avoid framework complexity unless the project already uses it.
- Make behavior explicit, testable, and easy to trace.
- Prefer simple DOM updates and clear event handlers.
